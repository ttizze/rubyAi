ソースコードはchrome extension viewerで見れる

deeplはdomの変更を監視し、変更があったdomをスクリプトに渡す。
そしてその要素の子要素を含めて再帰的に配列に入れ、uuidをdata属性に振る。
翻訳して帰ってきた結果は渡した配列と同じ結果になっているので、それをuuidと比較し、正しければdomを置き換える。

chrome翻訳は翻訳前の文字列をkey、翻訳後をvalueとしたマップを持っている。
そのため、翻訳前の文字列をkeyとしてマップから翻訳後の文字列を取得することで、翻訳後の文字列を取得できる。
そして置き換えてる。